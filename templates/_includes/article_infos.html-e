<p class="meta">
  <span class="byline author vcard">
    Publié par <span class="fn">{{ article.author }}</span>
  </span>
  {% include '_includes/article_time.html' %}
  {% if article.tags %}
  <span class="categories">
    {% for tag in article.tags %}
    <a class="category" href="{{ SITEURL }}/{{ tag.url }}">{{ tag }}</a>
    {% endfor %}
  </span>
  {% endif %}
</p>
